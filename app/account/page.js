export const metadata = {
  title: "Account",
  description: "Your account page.",
};

const AccountPage = () => {
  return (
    <div>
      <h1>Account Page</h1>
    </div>
  );
};

export default AccountPage;
